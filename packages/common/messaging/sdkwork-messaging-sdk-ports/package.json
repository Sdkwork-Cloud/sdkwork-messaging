{
  "name": "@sdkwork/messaging-sdk-ports",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "SDK client port contracts for SDKWork messaging.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src", "README.md"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "pnpm --dir ../../../.. exec vitest run packages/common/messaging/sdkwork-messaging-sdk-ports/tests --config vitest.config.ts --configLoader native --pool vmThreads"
  },
  "sdkwork": {
    "workspace": "sdkwork-messaging",
    "architecture": "common",
    "capability": "messaging-sdk-ports",
    "domain": "messaging",
    "packageType": "sdk-ports",
    "status": "standardizing"
  }
}
