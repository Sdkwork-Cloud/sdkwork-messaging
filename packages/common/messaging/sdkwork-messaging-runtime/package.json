{
  "name": "@sdkwork/messaging-runtime",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "SDKWork messaging runtime composition and feature flags.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src", "README.md"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "pnpm --dir ../../../.. exec vitest run packages/common/messaging/sdkwork-messaging-runtime/tests --config vitest.config.ts --configLoader native --pool vmThreads"
  },
  "dependencies": {
    "@sdkwork/messaging-contracts": "workspace:*",
    "@sdkwork/messaging-sdk-ports": "workspace:*",
    "@sdkwork/messaging-service": "workspace:*"
  },
  "sdkwork": {
    "workspace": "sdkwork-messaging",
    "architecture": "common",
    "capability": "messaging-runtime",
    "domain": "messaging",
    "packageType": "runtime",
    "status": "standardizing"
  }
}
