{
  "name": "@sdkwork/messaging-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Composable SDKWork messaging service facade built from generated SDK ports.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src", "README.md"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "pnpm --dir ../../../.. exec vitest run packages/common/messaging/sdkwork-messaging-service/tests --config vitest.config.ts --configLoader native --pool vmThreads"
  },
  "dependencies": {
    "@sdkwork/messaging-sdk-ports": "workspace:*"
  },
  "sdkwork": {
    "workspace": "sdkwork-messaging",
    "architecture": "common",
    "capability": "messaging-service",
    "domain": "messaging",
    "packageType": "service",
    "status": "standardizing"
  }
}
