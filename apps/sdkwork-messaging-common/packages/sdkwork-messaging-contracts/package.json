{
  "name": "@sdkwork/messaging-contracts",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Canonical SDKWork messaging contracts for external SMS and email delivery.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "pnpm --dir ../../../.. exec vitest run apps/sdkwork-messaging-common/packages/sdkwork-messaging-contracts/tests --config vitest.config.ts --configLoader native --pool vmThreads"
  },
  "sdkwork": {
    "workspace": "sdkwork-messaging",
    "architecture": "common",
    "capability": "messaging-contracts",
    "domain": "messaging",
    "packageType": "contracts",
    "status": "standardizing"
  }
}
