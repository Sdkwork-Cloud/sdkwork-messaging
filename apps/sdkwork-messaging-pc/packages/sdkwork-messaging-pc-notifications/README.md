# Messaging PC Notifications

Authenticated, server-paginated notification center feature. The package receives `NotificationCenterService`, owns feature state and i18n, and never constructs an SDK client.

