# Messaging PC Notifications

Public notification center feature with explicit anonymous and authenticated states. Anonymous access renders without a service port or personal data request; authenticated access receives `NotificationCenterService` for server pagination and read receipts. The package owns feature state and i18n and never constructs an SDK client.
