# Messaging PC Shell

Owns the flat product header, session-aware account controls, responsive navigation, and public application route composition. It maps the host-provided anonymous or authenticated session into the Notifications package and contains no SDK construction.
