# SDKs

SDK families owned by `sdkwork-messaging`:

- `sdkwork-messaging-app-sdk`
- `sdkwork-messaging-backend-sdk`

Run `pnpm sdk:materialize` before SDK generation to refresh OpenAPI authority and sdkgen inputs.
